# https://leetcode.com/problems/longest-consecutive-sequence/?envType=study-plan-v2&envId=top-interview-150

class Solution:
    def longestConsecutive(self, nums: List[int]) -> int:

        num_set = set(nums)
        longest = 0

        for n in num_set:
            if(n-1 not in num_set):
                length = 0
                while(n+length in num_set):
                    length += 1
                longest = max(longest, length)
            
        return longest

"""
Date: 21st Jan 2026
- Could not solve myself, hence saw soln soon
- Got TLE due to this rsn, be careful: https://chatgpt.com/s/t_697121ccf114819198e732e4b6a08dec
"""
